## 1. Project Initialization

- [x] 1.1 Initialize Vite project with React and TypeScript: `npm create vite ./ -- --template react-ts`
- [x] 1.2 Install dependencies and verify development server starts: `npm install && npm run dev`
- [x] 1.3 Clean up boilerplate and set up initial directory structure (`components/`, `pages/`, `assets/`).

## 2. Design System & Core Layout

- [x] 2.1 Implement `src/index.css` with the "Black and Gold" design system using CSS variables.
- [x] 2.2 Configure Google Fonts in `index.html` (Playfair Display, Inter, Cormorant Garamond).
- [x] 2.3 Create a `Layout` component with the global `Navigation` and `Footer` wrappers.

## 3. Global Components

- [x] 3.1 Build the `Navigation` component with sticky behavior and backdrop blur.
- [x] 3.2 Build the `Footer` component including social links and company contact info.
- [x] 3.3 Create a reusable `Button` component with gold-solid and gold-outline variants.

## 4. Homepage Implementation

- [x] 4.1 Implement the `Hero` section with video background support and animated typography.
- [x] 4.2 Implement the `ServiceGrid` and `ServiceCard` components with "gold border hover" effect.
- [x] 4.3 Implement the `WhyChoose` (feature pillars) and `TestimonialCarousel` sections.
- [x] 4.4 Implement the `ContactForm` with validation and Formspree integration.

## 5. Interior Pages

- [x] 5.1 Implement the `About` page (Mission, Vision, Team sections).
- [x] 5.2 Implement the `Projects` page with a masonry grid layout.
- [x] 5.3 Implement basic layouts for the `Blog` and `Contact` (full page) views.

## 6. Performance & SEO

- [x] 6.1 Add SEO meta tags and Title templates to the layout.
- [x] 6.2 Implement lazy loading for media and verify asset paths.
- [x] 6.3 Run final production build and validate Lighthouse performance scores.
